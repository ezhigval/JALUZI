# Local development: Astro on :4321, API on :3001
PUBLIC_API_URL=http://localhost:3001
SITE_URL=http://localhost:4321

# Production on one Ubuntu VM (same origin behind Caddy/nginx):
# leave PUBLIC_API_URL empty so the browser calls /api and /uploads
# PUBLIC_API_URL=
# SITE_URL=https://piter-jaluzi.ru
